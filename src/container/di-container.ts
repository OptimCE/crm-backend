import { Container } from "inversify";
/**
 * Global Dependency Injection Container.
 * Holds all bindings and resolves dependencies throughout the application.
 * @type {Container} Inversify container instance.
 */
export const container = new Container();
